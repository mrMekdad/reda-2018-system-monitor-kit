from setuptools import setup

setup(
    name="reda-2018-system-monitor-kit",
    version="0.1.0",
    description="Terminal-first monitor for processes, snapshots, and CSV reports.",
    author="Red@",
    packages=["system_monitor_kit"],
    package_dir={"": "src"},
)
